import React from "react";
import ReportsList from "./reports/ReportsList";

// PUBLIC_INTERFACE
export default function Reports() {
  /** Wrapper page to maintain backward link compatibility. */
  return <ReportsList />;
}
